{
  "name": "25th-codemod",
  "version": "1.0.0",
  "description": "Collection of code mods based on jscodeshift",
  "scripts": {
    "start": "jscodeshift",
    "lint": "eslint . || true",
    "fix": "eslint --fix . || true",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "eslint": "^2.2.0",
    "eslint-plugin-react": "^4.1.0"
  },
  "dependencies": {
    "eslint-config-airbnb": "^6.0.2"
  }
}
